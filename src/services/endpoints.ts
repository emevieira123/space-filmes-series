enum Endpoints {
  LOGIN = "/login",
  MOVIES = "/movies",
  MOVIE_ID = "/movies/:movieId",
  SERIES = "/series",
  SERIE_ID = "/series/:serieId",
  CATEGORIES = "/categories"
}

export default Endpoints;
